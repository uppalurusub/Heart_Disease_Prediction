import {useParams} from "react-router-dom"; import {useEffect,useState} from "react"; import {http} from "../api/http"; import {ResponseView} from "../components/common/ResponseView"; export const AnalysisPage=()=>{const {endpoint}=useParams(); const [data,setData]=useState<unknown>(),[error,setError]=useState(""); useEffect(()=>{http.get(`/heart/${endpoint}`).then(r=>setData(r.data)).catch(e=>setError(e.message))},[endpoint]); return <><h1>{endpoint?.replaceAll("-"," ")}</h1>{error&&<p className="error">{error}</p>}<ResponseView data={data}/></>};